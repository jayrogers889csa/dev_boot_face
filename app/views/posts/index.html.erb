<h1>Posts Home</h1>

<%= link_to 'New Post', new_post_path %>
	<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h4><%= link_to post.title, post_path(post) %></h4>
    <%= simple_format post.content %>
    <hr>
  <% end %>
<% end %>
